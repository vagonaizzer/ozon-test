server:
  host: "0.0.0.0"
  port: 8080

# Выбор хранилища: "memory" или "postgres"
storage:
  type: "memory"
  postgres:
    host: "localhost"
    port: 5432
    user: "postgres"
    password: "postgres"
    dbname: "ozonposts"
    sslmode: "disable"

logger:
  level: "info"
  env: "dev"
